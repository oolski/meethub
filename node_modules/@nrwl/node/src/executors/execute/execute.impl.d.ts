import 'dotenv/config';
import { ExecutorContext } from '@nrwl/devkit';
import { NodeBuildEvent } from '../build/build.impl';
export declare const enum InspectType {
    Inspect = "inspect",
    InspectBrk = "inspect-brk"
}
export interface NodeExecuteBuilderOptions {
    inspect: boolean | InspectType;
    runtimeArgs: string[];
    args: string[];
    waitUntilTargets: string[];
    buildTarget: string;
    host: string;
    port: number;
    watch: boolean;
}
export declare function executeExecutor(options: NodeExecuteBuilderOptions, context: ExecutorContext): AsyncGenerator<NodeBuildEvent, void, unknown>;
export default executeExecutor;
